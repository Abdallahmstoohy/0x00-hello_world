#include <stdio.h>
/**
 * main - this is the main baby
 * is this the main? yeah this is the main
 * Return: 0
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);
	putchar('\n');
	return (0);
}

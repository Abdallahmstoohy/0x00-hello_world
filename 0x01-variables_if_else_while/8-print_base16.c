#include <stdio.h>
/**
 * main - i hate this main tbh
 * let's see
 * Return: 0
 */
int main(void)
{
	int i;
	int c;

	for (i = 48; i < 58; i++)
		putchar(i);
	for (c = 97; c < 103; c++)
		putchar(c);
	putchar('\n');
	return (0);
}
